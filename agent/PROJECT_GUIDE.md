# RAG Agent Service - 프로젝트 가이드

## 📋 프로젝트 개요

이 프로젝트는 **멀티모달 RAG (Retrieval Augmented Generation) 시스템**으로, 건축도면, 기계설계도면 등 다양한 문서와 이미지를 처리하여 지능적인 질의응답 서비스를 제공합니다.

### 🎯 주요 목표

- 건축도면, 기계설계도면 등 전문 문서의 텍스트 추출 및 검색
- 저품질 스캔 이미지에서의 OCR 성능 최적화
- 한국어 최적화된 임베딩 모델을 통한 정확한 유사도 검색
- Google Gemini API를 활용한 고품질 답변 생성

## 🏗️ 시스템 아키텍처

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │    Backend      │    │     Agent       │
│   (SvelteKit)   │◄──►│   (NestJS)      │◄──►│   (FastAPI)     │
│                 │    │                 │    │                 │
│ - 파일 업로드    │    │ - API 게이트웨이 │    │ - 문서 처리     │
│ - 검색 인터페이스│    │ - 인증/권한     │    │ - 벡터 검색     │
│ - 결과 표시     │    │ - 데이터 관리   │    │ - LLM 통합      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │                         │
                              │                         │
                    ┌─────────────────┐    ┌─────────────────┐
                    │   PostgreSQL    │    │     Qdrant      │
                    │                 │    │                 │
                    │ - 메타데이터     │    │ - 벡터 저장소   │
                    │ - 사용자 데이터  │    │ - 유사도 검색   │
                    └─────────────────┘    └─────────────────┘
```

## 📁 프로젝트 구조

```
rag-system/
├── frontend/               # SvelteKit 프론트엔드
├── backend/               # NestJS 백엔드
├── agent/                 # FastAPI Agent 서비스 ⭐
│   ├── app/
│   │   ├── api/           # REST API 엔드포인트
│   │   │   └── v1/
│   │   │       ├── health.py      # 헬스체크 API
│   │   │       ├── documents.py   # 문서 업로드/관리 API
│   │   │       └── query.py       # 검색/질의 API
│   │   ├── core/          # 핵심 비즈니스 로직
│   │   │   ├── embedding_manager.py  # 임베딩 관리
│   │   │   ├── vector_store.py       # Qdrant 벡터 저장소
│   │   │   ├── gemini_service.py     # Gemini API 서비스
│   │   │   └── rag_engine.py         # RAG 엔진
│   │   ├── models/        # 데이터 모델
│   │   │   ├── schemas.py     # Pydantic 스키마
│   │   │   └── enums.py       # 열거형 정의
│   │   ├── services/      # 서비스 계층
│   │   │   └── document_processor.py # 문서 처리
│   │   ├── utils/         # 유틸리티
│   │   │   ├── logger.py      # 로깅 설정
│   │   │   └── file_handler.py # 파일 처리
│   │   └── main.py        # FastAPI 애플리케이션 진입점
│   ├── tests/             # 테스트
│   ├── config.py          # 설정 관리
│   ├── requirements.txt   # Python 의존성
│   ├── .env.example       # 환경변수 예제
│   ├── Dockerfile         # Docker 이미지
│   ├── docker-compose.yml # 서비스 오케스트레이션
│   └── README.md          # Agent 서비스 문서
└── docs/                  # 프로젝트 문서
```

## 🛠️ 기술 스택

### Agent 서비스 (현재 구현 완료)

| 구분                  | 기술                          | 버전     | 용도               |
| --------------------- | ----------------------------- | -------- | ------------------ |
| **Backend Framework** | FastAPI                       | 0.104.1  | REST API 서버      |
| **ASGI Server**       | Uvicorn                       | 0.24.0   | 비동기 웹서버      |
| **LLM**               | Google Gemini                 | 0.3.0    | 답변 생성          |
| **임베딩**            | sentence-transformers         | 2.2.2    | 텍스트 벡터화      |
| **벡터 DB**           | Qdrant                        | 1.7.0    | 벡터 검색          |
| **OCR**               | Tesseract, PaddleOCR, EasyOCR | -        | 이미지 텍스트 추출 |
| **이미지 처리**       | OpenCV                        | 4.6.0.66 | 이미지 전처리      |
| **문서 처리**         | PyMuPDF, PyPDF2               | -        | PDF 처리           |

### 전체 시스템 (계획)

- **Frontend**: SvelteKit + TailwindCSS + DaisyUI
- **Backend**: NestJS + PostgreSQL + TypeORM
- **컨테이너**: Docker + Docker Compose

## 🚀 설치 및 실행

### 1. 사전 요구사항

```bash
# Python 3.8+ 설치 확인
python --version

# Docker 설치 (선택사항)
docker --version
docker-compose --version
```

### 2. Agent 서비스 설치

```bash
# 프로젝트 클론
git clone <repository-url>
cd rag-system/agent

# Python 가상환경 생성 (권장)
python -m venv venv
source venv/bin/activate  # macOS/Linux
# 또는 venv\Scripts\activate  # Windows

# 의존성 설치
pip install -r requirements.txt
```

### 3. 환경 설정

```bash
# 환경변수 파일 생성
cp .env.example .env

# .env 파일 편집 (필수 설정)
vim .env
```

**필수 환경변수**:

```bash
# Google Gemini API 키 (필수)
GEMINI_API_KEY=your_actual_gemini_api_key_here

# Qdrant 설정
QDRANT_HOST=localhost
QDRANT_PORT=6333

# 기본 설정
DEBUG=true
APP_NAME=RAG Agent Service
```

### 4. Qdrant 벡터 데이터베이스 실행

**Option 1: Docker 사용 (권장)**

```bash
docker run -p 6333:6333 -v $(pwd)/qdrant_storage:/qdrant/storage qdrant/qdrant
```

**Option 2: Docker Compose 사용**

```bash
docker-compose up qdrant -d
```

### 5. Agent 서비스 실행

FastAPI는 ASGI 기반 비동기 웹 프레임워크이므로 **Uvicorn** ASGI 서버가 필요합니다.

**개발 모드** (자동 리로드 포함):

```bash
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**프로덕션 모드**:

```bash
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

**또는 Python으로 직접 실행** (main.py에서 uvicorn.run() 사용):

```bash
python app/main.py
```

**Docker Compose로 전체 서비스 실행**:

```bash
docker-compose up -d
```

### 6. 서비스 확인

- **API 문서**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **헬스체크**: http://localhost:8000/health
- **Qdrant 대시보드**: http://localhost:6333/dashboard

## 📊 주요 기능

### 1. 문서 업로드 및 처리

**지원 파일 형식**:

- **텍스트**: `.txt`, `.md`
- **PDF**: `.pdf` (텍스트 + 이미지 혼재)
- **이미지**: `.png`, `.jpg`, `.jpeg`, `.tiff`, `.bmp`
- **문서**: `.docx` (향후 지원)

**처리 과정**:

1. 파일 업로드 → 2. 형식별 파싱 → 3. OCR 처리 → 4. 텍스트 청킹 → 5. 임베딩 생성 → 6. 벡터 저장

### 2. 멀티모달 OCR 처리

**OCR 엔진**:

- **Tesseract**: 일반 문서용
- **PaddleOCR**: 다국어 지원, 손글씨 인식
- **EasyOCR**: 저품질 이미지 특화

**이미지 전처리**:

- 노이즈 제거, 대비 향상
- 기울어짐 보정, 해상도 향상
- 건축/기계도면 최적화

### 3. 벡터 검색

**임베딩 모델**:

- **한국어**: `jhgan/ko-sroberta-multitask`
- **멀티모달**: `clip-ViT-B-32`
- **영어**: `all-MiniLM-L6-v2`

**검색 방식**:

- 의미적 유사도 검색
- 하이브리드 검색 (키워드 + 벡터)
- 필터링 및 가중치 적용

### 4. LLM 기반 답변 생성

**Gemini API 통합**:

- 컨텍스트 기반 답변 생성
- 한국어 최적화
- 토큰 사용량 최적화

## 🔧 API 사용 예제

### 문서 업로드

```bash
curl -X POST "http://localhost:8000/api/v1/documents/upload" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@건축도면.pdf" \
  -F "metadata={\"title\":\"건축도면 A동\",\"tags\":[\"건축\",\"도면\",\"A동\"]}"
```

### 벡터 검색

```bash
curl -X POST "http://localhost:8000/api/v1/query/vector-search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "화장실 위치가 어디인가요?",
    "limit": 5,
    "score_threshold": 0.7
  }'
```

### RAG 질의

```bash
curl -X POST "http://localhost:8000/api/v1/query/search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "A동 3층 화장실 개수는?",
    "limit": 3,
    "use_gemini": true,
    "include_metadata": true
  }'
```

## 🧪 테스트

```bash
# 전체 테스트 실행
pytest tests/

# 특정 모듈 테스트
pytest tests/test_embedding.py -v

# 커버리지 확인
pytest --cov=app tests/
```

## 📈 성능 최적화

### 1. 임베딩 캐싱

- Redis를 활용한 임베딩 결과 캐싱
- 중복 문서 처리 시간 단축

### 2. 배치 처리

- 대용량 파일의 청크 단위 처리
- 메모리 효율적인 스트리밍 처리

### 3. 비동기 처리

- FastAPI의 async/await 활용
- 동시 요청 처리 성능 향상

## 🔒 보안 고려사항

### 1. API 키 관리

- 환경변수로 민감정보 관리
- `.env` 파일 버전관리 제외

### 2. 파일 업로드 제한

- 파일 크기 제한 (기본: 50MB)
- 허용 확장자 화이트리스트
- 바이러스 스캔 연동 (선택사항)

### 3. 입력 검증

- Pydantic을 통한 엄격한 타입 검증
- SQL 인젝션 방지
- XSS 공격 방지

## 🚨 문제 해결

### 1. 자주 발생하는 문제

**Qdrant 연결 실패**

```bash
# Qdrant 서비스 상태 확인
curl http://localhost:6333/health

# Docker 컨테이너 상태 확인
docker ps | grep qdrant
```

**Gemini API 오류**

```bash
# API 키 확인
echo $GEMINI_API_KEY

# API 할당량 확인
curl -H "x-goog-api-key: $GEMINI_API_KEY" \
  https://generativelanguage.googleapis.com/v1/models
```

**메모리 부족**

- 청크 크기 조정: `CHUNK_SIZE=500` (기본: 1000)
- 배치 크기 감소: `BATCH_SIZE=5` (기본: 10)

### 2. 로그 확인

```bash
# 실시간 로그 모니터링
tail -f logs/app.log

# 에러 로그만 필터링
grep -i "error" logs/app.log
```

## 🔄 배포

### 1. Docker 배포

```bash
# 이미지 빌드
docker build -t rag-agent:latest .

# 컨테이너 실행
docker run -p 8000:8000 --env-file .env rag-agent:latest
```

### 2. Docker Compose 배포

```bash
# 프로덕션 환경 실행
docker-compose -f docker-compose.prod.yml up -d

# 서비스 스케일링
docker-compose up --scale agent=3 -d
```

### 3. 클라우드 배포

**AWS ECS, Google Cloud Run, Azure Container Instances** 등에서 컨테이너 기반 배포 가능

## 📋 로드맵

### Phase 1 (완료) ✅

- [x] Agent 서비스 기본 구조
- [x] 멀티모달 문서 처리
- [x] Qdrant 벡터 검색
- [x] Gemini API 통합

### Phase 2 (개발 중)

- [ ] Frontend (SvelteKit) 개발
- [ ] Backend (NestJS) 개발
- [ ] 사용자 인증 및 권한 관리
- [ ] 대시보드 및 모니터링

### Phase 3 (계획)

- [ ] 고급 검색 기능
- [ ] 멀티테넌시 지원
- [ ] 성능 최적화
- [ ] 배포 자동화

## 🤝 기여하기

1. Repository Fork
2. Feature Branch 생성 (`git checkout -b feature/amazing-feature`)
3. 변경사항 Commit (`git commit -m 'Add amazing feature'`)
4. Branch Push (`git push origin feature/amazing-feature`)
5. Pull Request 생성

## 📄 라이선스

이 프로젝트는 MIT 라이선스를 따릅니다. 자세한 내용은 `LICENSE` 파일을 참조하세요.

---

**📞 연락처**: 프로젝트 관련 문의사항이나 이슈는 GitHub Issues를 통해 제보해 주세요.
